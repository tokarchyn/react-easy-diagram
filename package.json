{
  "private": true,
  "author": "Vitalii Tokarchyn",
  "license": "MIT",
  "workspaces": [
    "website",
    "lib_core"
  ],
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "scripts": {
    "start": "yarn workspace @react-easy-diagram/core build:dev && concurrently \"yarn workspace @react-easy-diagram/core start\" \"yarn workspace @react-easy-diagram/website start\"",
    "build:lib": "yarn workspace @react-easy-diagram/core run build:prod",
    "build:website": "yarn workspace @react-easy-diagram/website run build",
    "build": "yarn build:lib && yarn build:website"
  }
}
